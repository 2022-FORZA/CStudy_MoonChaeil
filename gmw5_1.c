#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#define MAX 45000

int main() {
	int t, a, b, i = 2, k, l, m[MAX]={0}, n[MAX]={0};
	scanf("%d", &t);

	while (t) {   //t만큼 a b 받기
		scanf("%d %d", &a, &b);
		for (int j = 2; j <= a; j++) {
			if((a%j)==0)

		}


		t--;
		printf("%d\n", k);
		k = 1;
	}
}